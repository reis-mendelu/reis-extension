node_modules
dist
.output
.wxt
coverage
package-lock.json
.agent
*.md
# Generated + committed minified by reis-data/scripts/buildMapData.mjs
src/data/map/rooms-index.json
# Same: written minified by scripts/fetch-campus-paths.mjs. Nobody reads it —
# the generator writes it and the tests assert its shape — so pretty-printing
# it only makes the committed file several times larger.
src/data/map/campusPaths.json
# Written by `infisical init` — reformatting it just loses to the next init
.infisical.json
# Captured IS Mendelu HTML. These are evidence of what IS actually emits, and
# CLAUDE.md's Parser Rules make a real sample the only thing that justifies a
# parser change — so they stay byte-for-byte as scraped. Reformatting them
# would validate future parser work against markup IS never sends.
src/api/__tests__/fixtures/**/*.html
# The IS Mendelu HTML parsers. CLAUDE.md's Parser Rules freeze them: they are
# extremely brittle, column-index constants are load-bearing, and a change is
# only justified by a real captured IS sample. Even behaviour-preserving
# reformatting is churn in the files where churn is most expensive, so they are
# left exactly as written. Their tests are ordinary code and stay formatted.
src/utils/parsers/**/*.ts
!src/utils/parsers/**/__tests__/**
