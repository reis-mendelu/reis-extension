export const ISKAM_ENABLED = true;

// Document notes (card-first note editor + one-way Drive notes backup) are
// dormant: kept in the codebase for a possible future return, but with no UI
// entry point and fully out of the sync. Flip to true to wake the feature.
export const NOTES_ENABLED = false;
